name: AI指令助手
on:
  issue_comment:
    types: [created]

jobs:
  run-command:
    if: contains(github.event.comment.body, '@claude')
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: 执行AI指令
        run: echo "✅ 已收到 @claude 指令，正在处理..."
